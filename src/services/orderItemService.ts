import { IOrderItem } from "../types/OrderData";
import OrderItem, { OrderItemDocument } from "../model/OrderItem";

/* export const createOrderItem = async (
  newOrderItem: IOrderItem
): Promise<OrderItemDocument | null> => {
 
   /*  const orderItem = new OrderItem();
    orderItem.carId = 
  return null;
};
 */
